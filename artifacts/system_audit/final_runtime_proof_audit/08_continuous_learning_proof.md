# 08 CONTINUOUS LEARNING PROOF

- Feedback Storage: User ratings and comments submitted via `POST /api/admin/assistant/feedback` are persisted in `admin_assistant_feedback` table.
- Learning Review Queue: Candidates enter the continuous learning queue for Admin review before inclusion in future dataset manifests (`CONNECTED`).
