# 07 DATASET TO RAG TRACE

- Data Bridge: Approved documents and dataset records are extracted and indexed into `RagRepository` collections.
- RAG Workspace Pages: `RagPage.jsx` and `RagSandboxPage.jsx` connect to `GET /api/admin/rag` and `POST /api/admin/rag-sandbox` (`LEVEL 5 - E2E VERIFIED`).
