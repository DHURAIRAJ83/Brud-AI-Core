# 12 TOKEN BINDING VALIDATION AUDIT

- Hash Bindings: Verified strict matching for `dataset_manifest_hash`, `model_hash`, `tokenizer_hash`, `release_id`, `snapshot_hash`, and `tenant_id`. Hash mismatch strictly fails closed.
