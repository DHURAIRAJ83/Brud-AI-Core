# 05 PRIVATE MODEL / PROVIDER TRACE

- Provider Adapters: Ollama, Gemini, OpenRouter, Groq, Claude, BrudMiniLLM.
- Provider Settings Dashboard: `ProviderSettingsTab.jsx` connects to `/api/admin/mini-brain/provider-settings` (`LEVEL 5 - E2E VERIFIED`).
- Secret Exposure Safety: Zero API keys, raw HMAC secrets, or private signing keys exposed in logs, API responses, or JSON manifests.
