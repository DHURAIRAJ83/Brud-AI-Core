"""Placeholder FAQ module for the Trusted Web / Deterministic Tools
Admin Assistant help topic. The real, bilingual FAQ content is part of
the Smart Routing commit group and will replace this stub in that
commit -- this file exists only so admin_assistant_chat_service.py's
import succeeds before that group lands."""

TRUSTED_WEB_FAQ: dict[str, dict] = {}


def match_trusted_web_question(message: str) -> str | None:
    """Stub: no FAQ entries registered yet. Always returns None until
    the Smart Routing commit group replaces this file."""

    return None
